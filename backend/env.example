# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/bonus_calc

# SQL Server Configuration for Performance Data
# Replace these placeholder values with your actual SQL Server credentials
SQL_SERVER_HOST=your_sql_server_host
SQL_SERVER_DATABASE=your_database_name
SQL_SERVER_USERNAME=your_username
SQL_SERVER_PASSWORD=your_password
SQL_SERVER_DRIVER={SQL Server}
SQL_SERVER_PORT=1433

# Alternative SQL Server Driver (if using ODBC Driver 17)
# SQL_SERVER_DRIVER={ODBC Driver 17 for SQL Server}

# Instructions:
# 1. Copy this file to .env in the backend directory
# 2. Replace the placeholder values with your actual SQL Server credentials
# 3. For the Servicing Team query, the system will use the CollectionDetail table
# 4. The query filters by Country = 'ESPANA' and date range based on selected quarter
# 5. Make sure your SQL Server is accessible from the backend server 